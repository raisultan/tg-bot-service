version: '3.8'

services:
    bot:
        command: sh -c "uvicorn app.main:app --reload --host 0.0.0.0"
        build:
            context: ./bot
            dockerfile: Dockerfile
        ports:
          - 8001:8000
        volumes:
          - ./bot:/bot
